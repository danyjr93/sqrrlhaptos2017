// import { VisibilityFilters } from './actions'

const initialState = {
}

export default function main(state, action) {
  switch(action.type){
    case "SOME_ACTION":

      return state;
    default:
      return initialState;
  }
}
